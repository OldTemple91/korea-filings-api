<?xml version="1.0" encoding="UTF-8"?>
<!--
  Korea Filings landing sitemap.

  Human-readable pages only — currently exactly one. Earlier revisions
  also listed the API host's llms.txt and /v1/pricing, but Search
  Console correctly classified both as "crawled, not indexed": they are
  machine surfaces (plain text / JSON) with nothing to show in a search
  result. Listing them only produced standing GSC warnings and spent
  crawl budget on URLs that can never rank, so they are gone.

  The API host keeps its own sitemap at
  https://api.koreafilings.com/sitemap.xml for indexers and x402
  crawlers that consume those surfaces directly — that audience does
  not arrive via Google.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://koreafilings.com/</loc>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>
