# package.json `files` whitelist already controls what ships,
# but .npmignore is a defence-in-depth layer for the bits that
# slip through default packing rules.
src/
tests/
examples/
tsconfig.json
tsup.config.ts
.gitignore
.DS_Store
*.log
coverage/
